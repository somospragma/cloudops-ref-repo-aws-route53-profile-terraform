# =============================================================================
# Configuración de Providers
# PC-IAC-005: El provider se inyecta desde el Root mediante aws.project
# =============================================================================

# El provider aws.project es inyectado por el consumidor (Root) al invocar este módulo.
# No se declara aquí — solo se consume via configuration_aliases en versions.tf.
